Big Data HW2

Sbt execution steps:
	./sbt/bin/sbt clean
	./sbt/bin/sbt package
	./sbt/bin/sbt "run"

Steps to execute the HW:
	For each part there is a separate mainClass file in simple.sbt. Uncomment it and comment the rest.

To view any output:
	Check the output folder

The Src directory has the source code

NOTE: The dataset must be loaded in the INPUT_DIR and the userdata file should be loaded in INPUT_INFO_DIR
